def find_subarray(a):
	for i in range(len(a)):
		sum = 0
		for j in range(i, len(a)):
			sum += a[j]
			if sum == 0:
				return 'subarray exists'
	
a = [8, 2, -1, 1, 4, 2, 7]
print(find_subarray(a))	
