def find_duplicates(a):
	s =set()
	for x in a:
		if x in s:
			return 'found duplicates at index '
		s.add(x)

if __name__ == '__main__':
	a = [10, 20, 30, 40, 50, 40]
	print(find_duplicates(a))
	
	
	#Time complexity O(n)
	#Space complexity O(n)
