def find_subarray(a):
	for i in range(len(a)):
		sum = 0
		for j in range(i, len(a)):
			sum += a[j]
			if sum == 0:
				print( 'sublists', a[i : j+1])
	
a = [6, -3, -3, -1, 1, 4, 2, -2]
print(find_subarray(a))	
